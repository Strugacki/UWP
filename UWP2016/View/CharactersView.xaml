<Page
    x:Class="UWP2016.View.CharactersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP2016.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/gotback.jpg"
                    Stretch="UniformToFill" />
        </Grid.Background>
        <StackPanel>
            <TextBlock Text="Character Name: " Height="30" Width="263" FontSize="18" HorizontalAlignment="Left" Padding="65,0,0,0"/>
            <TextBox Name="CharacterToSearch" Text="{Binding Name, Mode=TwoWay}" Height="30" HorizontalAlignment="Left" Width="263"/>
            <Button Name="SearchCharacters" Command="{Binding GetCharacterCommand}" Content="Search" Height="30" Width="263" FontSize="12" />
            <TextBox Text="{Binding Father, Mode=TwoWay}" Height="30" HorizontalAlignment="Left" Width="263"/>
            <TextBox Text="{Binding Mother, Mode=TwoWay}" Height="30" HorizontalAlignment="Left" Width="263"/>
        </StackPanel>
    </Grid>
</Page>
